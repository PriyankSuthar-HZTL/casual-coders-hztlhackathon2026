module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/http2 [external] (http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http2", () => require("http2"));

module.exports = mod;
}),
"[externals]/assert [external] (assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("assert", () => require("assert"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/string_decoder [external] (string_decoder, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("string_decoder", () => require("string_decoder"));

module.exports = mod;
}),
"[externals]/node:assert [external] (node:assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:assert", () => require("node:assert"));

module.exports = mod;
}),
"[externals]/node:net [external] (node:net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:net", () => require("node:net"));

module.exports = mod;
}),
"[externals]/node:http [external] (node:http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http", () => require("node:http"));

module.exports = mod;
}),
"[externals]/node:querystring [external] (node:querystring, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:querystring", () => require("node:querystring"));

module.exports = mod;
}),
"[externals]/node:events [external] (node:events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:events", () => require("node:events"));

module.exports = mod;
}),
"[externals]/node:diagnostics_channel [external] (node:diagnostics_channel, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:diagnostics_channel", () => require("node:diagnostics_channel"));

module.exports = mod;
}),
"[externals]/node:util [external] (node:util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util", () => require("node:util"));

module.exports = mod;
}),
"[externals]/node:tls [external] (node:tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:tls", () => require("node:tls"));

module.exports = mod;
}),
"[externals]/node:buffer [external] (node:buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:buffer", () => require("node:buffer"));

module.exports = mod;
}),
"[externals]/node:zlib [external] (node:zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:zlib", () => require("node:zlib"));

module.exports = mod;
}),
"[externals]/node:perf_hooks [external] (node:perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:perf_hooks", () => require("node:perf_hooks"));

module.exports = mod;
}),
"[externals]/node:util/types [external] (node:util/types, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util/types", () => require("node:util/types"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/node:worker_threads [external] (node:worker_threads, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:worker_threads", () => require("node:worker_threads"));

module.exports = mod;
}),
"[externals]/node:http2 [external] (node:http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http2", () => require("node:http2"));

module.exports = mod;
}),
"[externals]/node:url [external] (node:url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:url", () => require("node:url"));

module.exports = mod;
}),
"[externals]/node:async_hooks [external] (node:async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:async_hooks", () => require("node:async_hooks"));

module.exports = mod;
}),
"[externals]/node:console [external] (node:console, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:console", () => require("node:console"));

module.exports = mod;
}),
"[externals]/node:fs/promises [external] (node:fs/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:fs/promises", () => require("node:fs/promises"));

module.exports = mod;
}),
"[externals]/node:path [external] (node:path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:path", () => require("node:path"));

module.exports = mod;
}),
"[externals]/node:timers [external] (node:timers, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:timers", () => require("node:timers"));

module.exports = mod;
}),
"[externals]/node:dns [external] (node:dns, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:dns", () => require("node:dns"));

module.exports = mod;
}),
"[project]/src/lib/detector.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "detectComponents",
    ()=>detectComponents
]);
// ——————————————————————————————————————
//  Stackshift — structural component detector v3
//
//  Principles:
//  1. Every <section> is a component candidate. Also: main > * and [data-component].
//  2. <header>, <nav>, <footer> are chrome, not content — skip them entirely.
//  3. Classification is purely structural. No hardcoded IDs, class names, or
//     keywords specific to any particular site.
//  4. Signals score DOM shape: element counts, nesting, presence of media,
//     repetition, ARIA/semantic hints.
// ——————————————————————————————————————
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/load-parse.js [app-route] (ecmascript)");
;
// —— helpers ——
const text = ($el)=>$el.text().trim().replace(/\s+/g, " ");
const abs = (u, base)=>{
    if (!u) return "";
    try {
        return new URL(u, base).toString();
    } catch  {
        return u;
    }
};
const snakeCase = (s)=>s.toLowerCase().replace(/[^a-z0-9]+/g, "_").replace(/^_|_$/g, "").slice(0, 40) || "field";
const field = (displayName, type, value, opts = {})=>({
        uid: snakeCase(displayName),
        displayName,
        type,
        value,
        ...opts
    });
// True if the node is (or lives inside) chrome we want to exclude.
const isChrome = ($n)=>$n.is("header, nav, footer") || $n.parents("header, nav, footer").length > 0;
// Count direct-or-near-direct "item" children that share a shape.
// Looks at children of depth 1 and 2 so grid wrappers don't hide the items.
function repeatingItems($, $n, itemSelector, shapeFn) {
    let best = null;
    const containers = $n.find("div, ul, ol, section").addBack();
    containers.each((_, container)=>{
        const $c = $(container);
        const kids = $c.children(itemSelector);
        if (kids.length < 2) return;
        const matching = kids.filter((_, el)=>shapeFn($(el)));
        if (matching.length >= 2 && matching.length >= kids.length * 0.6 && (!best || matching.length > best.length)) {
            best = matching;
        }
    });
    return best ?? $n.filter(()=>false);
}
// Finds repeating card-shaped elements (article first, else divs/lis with heading).
function findCards($, $n) {
    const articles = $n.find("article").filter((_, el)=>$(el).find("h1, h2, h3, h4").length > 0 && ($(el).find("p").length > 0 || $(el).find("img, picture").length > 0));
    if (articles.length >= 2) return articles;
    return repeatingItems($, $n, "div, li, article", (el)=>el.find("h1, h2, h3, h4").length > 0 && (el.find("p, span").length > 0 || el.find("img").length > 0));
}
// Finds accordion-style repeating items.
function findAccordionItems($, $n) {
    // 1. Native <details>/<summary>.
    const details = $n.find("details");
    if (details.length >= 2) return details;
    // 2. ARIA pattern: buttons with aria-expanded next to a region/panel.
    const ariaBtns = $n.find("[aria-expanded]");
    if (ariaBtns.length >= 2) return ariaBtns;
    // 3. Heading + sibling content pattern inside a repeating container.
    return repeatingItems($, $n, "div, li", (el)=>el.find("h2, h3, h4, h5, button, summary").length > 0 && el.find("p, div, ul").length > 0 && text(el).length > 20);
}
// Finds repeating "feature-ish" items: icon/image + title + short body.
function findFeatureItems($, $n) {
    return repeatingItems($, $n, "div, li", (el)=>el.find("svg, img, i[class*='icon'], [class*='icon']").length > 0 && el.find("h1, h2, h3, h4, h5, strong").length > 0);
}
// —— rules (purely structural scoring) ——
const RULES = [
    // ——— Hero ———
    {
        kind: "hero",
        name: "Hero",
        signals: [
            {
                weight: 0.7,
                test: (_, $n)=>$n.find("h1").length > 0 ? 1 : 0
            },
            // CTA pair often present in a hero
            {
                weight: 0.15,
                test: ($, $n)=>{
                    const btnGroup = $n.find("div").filter((_, el)=>$(el).children("a, button").length >= 2);
                    return btnGroup.length > 0 ? 1 : 0;
                }
            },
            // Decorative/background/hero imagery
            {
                weight: 0.15,
                test: ($, $n)=>{
                    const bg = $n.find("div").filter((_, el)=>/absolute|fixed|inset-0|bg-|cover|hero/i.test($(el).attr("class") ?? "")).find("img, picture").length;
                    return bg > 0 || $n.find("img, picture").length === 1 ? 1 : 0;
                }
            }
        ],
        extract: ($, $n, base)=>{
            const h1 = $n.find("h1").first();
            const headline = text(h1);
            const badge = text($n.find("span, div, p").filter((_, el)=>{
                const $el = $(el);
                if ($el.parents("h1, h2, h3").length) return false;
                const t = text($el);
                return t.length > 0 && t.length < 60;
            }).first());
            const subtitle = text($n.find("p").filter((_, el)=>text($(el)).length > 20).first());
            const ctas = $n.find("a[href], button").map((_, el)=>({
                    title: text($(el)),
                    href: abs($(el).attr("href"), base)
                })).get().filter((c)=>c.title).slice(0, 3);
            const imgSrc = abs($n.find("img, picture img").first().attr("src"), base);
            const out = [
                field("Headline", "single_line", headline, {
                    required: true
                })
            ];
            if (badge && badge !== headline) out.push(field("Badge", "single_line", badge));
            if (subtitle) out.push(field("Subtitle", "multi_line", subtitle));
            if (ctas[0]) out.push(field("Primary CTA", "url", ctas[0]));
            if (ctas[1]) out.push(field("Secondary CTA", "url", ctas[1]));
            if (imgSrc) out.push(field("Image", "file", imgSrc, {
                assetUrl: imgSrc
            }));
            return out;
        }
    },
    // ——— FAQ / Accordion ———
    {
        kind: "faq",
        name: "FAQ",
        signals: [
            // Native accordion is the strongest signal.
            {
                weight: 0.8,
                test: (_, $n)=>$n.find("details").length >= 2 ? 1 : 0
            },
            // ARIA accordion pattern.
            {
                weight: 0.7,
                test: (_, $n)=>$n.find("[aria-expanded]").length >= 2 ? 1 : 0
            },
            // Repeating Q+A as heading/button + following body.
            {
                weight: 0.5,
                test: ($, $n)=>findAccordionItems($, $n).length >= 3 ? 1 : 0
            }
        ],
        extract: ($, $n)=>{
            const items = [];
            const details = $n.find("details");
            if (details.length >= 2) {
                details.each((_, el)=>{
                    const $el = $(el);
                    const q = text($el.find("summary").first());
                    const a = text($el).replace(q, "").trim();
                    if (q) items.push({
                        question: q,
                        answer: a
                    });
                });
            } else {
                const ariaBtns = $n.find("[aria-expanded]");
                if (ariaBtns.length >= 2) {
                    ariaBtns.each((_, el)=>{
                        const $el = $(el);
                        const q = text($el);
                        const panelId = $el.attr("aria-controls");
                        const panel = panelId ? $(`#${panelId}`) : $el.next();
                        const a = text(panel);
                        if (q) items.push({
                            question: q,
                            answer: a
                        });
                    });
                } else {
                    const accItems = findAccordionItems($, $n);
                    accItems.each((_, el)=>{
                        const $el = $(el);
                        const q = text($el.find("h2, h3, h4, h5, button, summary").first());
                        const a = text($el.find("p, div").last());
                        if (q) items.push({
                            question: q,
                            answer: a
                        });
                    });
                }
            }
            return [
                field("Section Title", "single_line", text($n.find("h2").first()) || "FAQ"),
                field("Items", "group", items, {
                    itemSchema: [
                        field("Question", "single_line", ""),
                        field("Answer", "rich_text", "")
                    ]
                })
            ];
        }
    },
    // ——— Testimonials ———
    {
        kind: "testimonial",
        name: "Testimonials",
        signals: [
            {
                weight: 0.8,
                test: (_, $n)=>{
                    const n = $n.find("blockquote, q").length;
                    return n >= 3 ? 1 : n === 2 ? 0.8 : n === 1 ? 0.4 : 0;
                }
            },
            // <cite> attribution
            {
                weight: 0.3,
                test: (_, $n)=>$n.find("cite").length > 0 ? 1 : 0
            },
            // Repeating quote-like items (rounded avatar + short paragraph of text)
            {
                weight: 0.3,
                test: ($, $n)=>{
                    const rounded = $n.find("img").filter((_, el)=>{
                        const cls = $(el).attr("class") ?? "";
                        return /rounded-full|avatar|circle/i.test(cls);
                    });
                    return rounded.length >= 2 ? 1 : 0;
                }
            }
        ],
        extract: ($, $n)=>{
            const sectionTitle = text($n.find("h2").first());
            const quotes = $n.find("blockquote, q");
            let items = [];
            if (quotes.length > 0) {
                items = quotes.map((_, el)=>{
                    const $bq = $(el);
                    const quote = text($bq);
                    const $card = $bq.closest("div, article, li, figure");
                    const $cite = $card.find("cite, figcaption").first();
                    const citeText = text($cite);
                    const [author_name = "", role = ""] = citeText.split(/[,—–·]/).map((s)=>s.trim());
                    const avatar = abs($card.find("img").first().attr("src"), "");
                    return {
                        quote,
                        author_name,
                        role,
                        avatar
                    };
                }).get();
            }
            return [
                ...sectionTitle ? [
                    field("Section Title", "single_line", sectionTitle)
                ] : [],
                field("Testimonials", "group", items, {
                    itemSchema: [
                        field("Quote", "multi_line", ""),
                        field("Author Name", "single_line", ""),
                        field("Role", "single_line", ""),
                        field("Avatar", "file", "")
                    ]
                })
            ];
        }
    },
    // ——— Media Gallery (video/iframe or many images) ———
    {
        kind: "media_gallery",
        name: "Media",
        signals: [
            {
                weight: 0.8,
                test: (_, $n)=>$n.find("iframe, video").length > 0 ? 1 : 0
            },
            // Grid of images (not inside cards)
            {
                weight: 0.5,
                test: ($, $n)=>{
                    const imgs = $n.find("img").filter((_, el)=>{
                        return $(el).parents("article").length === 0;
                    });
                    return imgs.length >= 4 ? 1 : imgs.length === 3 ? 0.6 : 0;
                }
            },
            // No cards / no blockquotes (guard against misclassification)
            {
                weight: 0.15,
                test: (_, $n)=>$n.find("article, blockquote").length === 0 ? 1 : 0
            }
        ],
        extract: ($, $n, base)=>{
            const title = text($n.find("h2, h3").first());
            const description = text($n.find("p").filter((_, el)=>text($(el)).length > 10).first());
            const iframeSrc = abs($n.find("iframe").first().attr("src"), base);
            const videoSrc = abs($n.find("video source").first().attr("src"), base);
            const mediaSrc = iframeSrc || videoSrc;
            const images = $n.find("img").map((_, el)=>abs($(el).attr("src"), base)).get().filter(Boolean);
            const out = [];
            if (title) out.push(field("Title", "single_line", title));
            if (description) out.push(field("Description", "multi_line", description));
            if (mediaSrc) out.push(field("Media URL", "url", {
                title: "Watch",
                href: mediaSrc
            }));
            if (images.length > 1) {
                out.push(field("Images", "group", images.map((src)=>({
                        image: src
                    })), {
                    itemSchema: [
                        field("Image", "file", "")
                    ]
                }));
            }
            return out;
        }
    },
    // ——— Card Grid ———
    {
        kind: "card_grid",
        name: "Card Grid",
        signals: [
            // Canonical: multiple <article> cards with image + heading
            {
                weight: 0.8,
                test: ($, $n)=>{
                    const cards = $n.find("article").filter((_, el)=>$(el).find("h1, h2, h3, h4").length > 0 && ($(el).find("img, picture").length > 0 || $(el).find("p").length > 0));
                    return cards.length >= 3 ? 1 : cards.length >= 2 ? 0.8 : 0;
                }
            },
            // Repeating non-article cards
            {
                weight: 0.5,
                test: ($, $n)=>{
                    const cards = findCards($, $n);
                    return cards.length >= 3 ? 1 : cards.length >= 2 ? 0.7 : 0;
                }
            }
        ],
        extract: ($, $n, base)=>{
            let cardEls = $n.find("article").filter((_, el)=>$(el).find("h1, h2, h3, h4").length > 0);
            if (cardEls.length < 2) {
                const found = findCards($, $n);
                if (found.length >= 2) cardEls = found;
            }
            const cards = cardEls.map((_, el)=>{
                const $el = $(el);
                const title = text($el.find("h1, h2, h3, h4").first());
                const image = abs($el.find("img, picture img").first().attr("src"), base);
                const link = abs($el.find("a").first().attr("href"), base);
                const excerpt = text($el.find("p").first());
                return {
                    title,
                    image,
                    link,
                    excerpt
                };
            }).get();
            const sectionTitle = text($n.find("h2").first());
            const sectionSub = text($n.find("p").filter((_, el)=>$(el).parents("article").length === 0).first());
            return [
                ...sectionTitle ? [
                    field("Section Title", "single_line", sectionTitle)
                ] : [],
                ...sectionSub ? [
                    field("Section Subtitle", "multi_line", sectionSub)
                ] : [],
                field("Cards", "group", cards, {
                    itemSchema: [
                        field("Title", "single_line", ""),
                        field("Image", "file", ""),
                        field("Link", "url", {
                            title: "",
                            href: ""
                        }),
                        field("Excerpt", "multi_line", "")
                    ]
                })
            ];
        }
    },
    // ——— Feature List ———
    {
        kind: "feature_list",
        name: "Feature List",
        signals: [
            // Repeating items with icon + heading
            {
                weight: 0.7,
                test: ($, $n)=>{
                    const items = findFeatureItems($, $n);
                    return items.length >= 3 ? 1 : items.length >= 2 ? 0.7 : 0;
                }
            },
            // Many short headings + short paragraphs (text-driven list)
            {
                weight: 0.3,
                test: (_, $n)=>{
                    const headings = $n.find("h3, h4").length;
                    const paras = $n.find("p").length;
                    return headings >= 3 && paras >= 3 && $n.find("article").length === 0 ? 1 : 0;
                }
            }
        ],
        extract: ($, $n, base)=>{
            let items = findFeatureItems($, $n);
            if (items.length < 2) {
                items = repeatingItems($, $n, "div, li", (el)=>el.find("h3, h4, h5, strong").length > 0 && el.find("p").length > 0);
            }
            const features = items.map((_, el)=>{
                const $el = $(el);
                const title = text($el.find("h1, h2, h3, h4, h5, strong").first());
                const body = text($el.find("p").first());
                const icon = abs($el.find("img").first().attr("src"), base);
                return {
                    title,
                    body,
                    ...icon ? {
                        icon
                    } : {}
                };
            }).get().filter((f)=>f.title);
            return [
                field("Section Title", "single_line", text($n.find("h2").first()) || "Features"),
                field("Features", "group", features, {
                    itemSchema: [
                        field("Title", "single_line", ""),
                        field("Body", "multi_line", ""),
                        field("Icon", "file", "")
                    ]
                })
            ];
        }
    },
    // ——— CTA Banner ———
    {
        kind: "cta_banner",
        name: "CTA Banner",
        signals: [
            // Short section with a heading and an action — and nothing else going on.
            {
                weight: 0.7,
                test: (_, $n)=>{
                    const hasAction = $n.find("a[href], button").length > 0;
                    const short = text($n).length < 500;
                    const noList = $n.find("article, blockquote, iframe, video, details").length === 0;
                    const headingOk = $n.find("h2, h3, strong").length > 0;
                    return hasAction && short && noList && headingOk ? 1 : 0;
                }
            },
            // Single heading + single CTA, slightly looser
            {
                weight: 0.3,
                test: (_, $n)=>$n.find("h2, h3").length === 1 && $n.find("a[href], button").length >= 1 && $n.find("article").length === 0 ? 1 : 0
            }
        ],
        extract: ($, $n, base)=>{
            const heading = text($n.find("h1, h2, h3, strong").first());
            const body = text($n.find("p").filter((_, el)=>text($(el)) !== heading).first());
            const btn = $n.find("a[href], button").first();
            return [
                ...heading ? [
                    field("Heading", "single_line", heading)
                ] : [],
                ...body ? [
                    field("Body", "multi_line", body)
                ] : [],
                field("CTA", "url", {
                    title: text(btn),
                    href: abs(btn.attr("href"), base)
                })
            ];
        }
    },
    // ——— Rich Text (fallback) ———
    {
        kind: "rich_text",
        name: "Content",
        signals: [
            {
                weight: 0.5,
                test: (_, $n)=>$n.find("p").length >= 2 && $n.find("article, blockquote, iframe, video, details").length === 0 ? 1 : 0
            },
            {
                weight: 0.3,
                test: (_, $n)=>text($n).length > 300 ? 1 : 0
            }
        ],
        extract: (_, $n)=>{
            const title = text($n.find("h1, h2, h3").first());
            const body = $n.html() ?? "";
            return [
                ...title ? [
                    field("Title", "single_line", title)
                ] : [],
                field("Body", "rich_text", body)
            ];
        }
    }
];
function detectComponents(html, baseUrl) {
    const $ = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
    const out = [];
    let counter = 0;
    // Strip chrome so its descendants don't polute candidate lists or text scoring.
    // We operate on a working copy via cheerio's selection; we don't mutate the DOM.
    // Candidates: sections first (most common content wrappers), then main > *,
    // explicit data-component markers, and article elements that live outside sections.
    const candidates = $([
        "section",
        "main > *",
        "[data-component]",
        "article"
    ].join(", ")).toArray().filter((el)=>{
        const $el = $(el);
        if (isChrome($el)) return false;
        // Skip elements that are themselves descendants of another section/article —
        // those get handled by the outer candidate.
        if ($el.parents("section, article, [data-component]").length > 0) return false;
        return true;
    });
    const seen = new Set();
    for (const el of candidates){
        if (seen.has(el)) continue;
        const $n = $(el);
        // Parent already claimed? skip.
        if ($n.parents().toArray().some((p)=>seen.has(p))) continue;
        // Skip near-empty nodes.
        const contentLength = text($n).length;
        const hasMedia = $n.find("img, picture, video, iframe, svg").length > 0;
        if (contentLength < 20 && !hasMedia) continue;
        // Score every rule; highest wins.
        const scored = RULES.map((rule)=>{
            const total = rule.signals.reduce((s, sig)=>s + sig.weight, 0) || 1;
            const score = rule.signals.reduce((acc, sig)=>acc + sig.weight * sig.test($, $n), 0) / total;
            return {
                rule,
                score
            };
        }).sort((a, b)=>b.score - a.score);
        const best = scored[0];
        if (!best || best.score < 0.25) continue;
        const fields = best.rule.extract($, $n, baseUrl);
        if (!fields.length) continue;
        // Prefer a detected text field as the preview label.
        const preview = fields.find((f)=>(f.type === "single_line" || f.type === "multi_line") && typeof f.value === "string" && f.value.length > 0)?.value ?? text($n).slice(0, 120) ?? best.rule.name;
        // UID derivation: prefer explicit id, then heading slug, then rule kind.
        const sectionId = $n.attr("id") ?? "";
        const h2Text = text($n.find("h2, h1").first());
        const suggestedUid = snakeCase(sectionId) || snakeCase(h2Text) || best.rule.kind;
        // Display name: heading text if short enough, otherwise the rule name.
        const name = h2Text && h2Text.length <= 50 ? h2Text : best.rule.name;
        counter += 1;
        seen.add(el);
        out.push({
            id: `c_${counter}_${best.rule.kind}`,
            kind: best.rule.kind,
            name,
            suggestedUid,
            confidence: Math.min(0.99, Math.max(0.35, best.score)),
            preview: String(preview).slice(0, 160),
            selector: buildSelector($n),
            fields,
            previewHtml: $.html($n)
        });
    }
    return out;
}
function buildSelector($n) {
    const el = $n[0];
    if (!el || el.type !== "tag") return "unknown";
    const tag = el.tagName.toLowerCase();
    const id = $n.attr("id");
    if (id) return `${tag}#${id}`;
    const dataComp = $n.attr("data-component");
    if (dataComp) return `${tag}[data-component="${dataComp}"]`;
    const cls = ($n.attr("class") ?? "").split(/\s+/).filter(Boolean)[0];
    return cls ? `${tag}.${cls}` : tag;
}
}),
"[project]/src/lib/llm-detector.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "detectComponentsHybrid",
    ()=>detectComponentsHybrid,
    "detectComponentsWithLlm",
    ()=>detectComponentsWithLlm,
    "runGateCheck",
    ()=>runGateCheck,
    "testGeminiApiKey",
    ()=>testGeminiApiKey
]);
// ——————————————————————————————————————
//  Stackshift — LLM-based component detector (Google Gemini)
// ——————————————————————————————————————
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/axios/lib/axios.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/load-parse.js [app-route] (ecmascript)");
;
;
// —— Constants ——
const VALID_KINDS = [
    "hero",
    "feature_list",
    "card_grid",
    "testimonial",
    "faq",
    "cta_banner",
    "rich_text",
    "media_gallery"
];
const VALID_FIELD_TYPES = [
    "single_line",
    "multi_line",
    "rich_text",
    "url",
    "file",
    "boolean",
    "number",
    "group",
    "reference"
];
// Minimum meaningful stripped-HTML length before we bother calling the LLM.
const MIN_HTML_CHARS = 400;
// Retry / backoff (free tier = 15 RPM).
const MAX_RETRIES = 3;
const BASE_BACKOFF_MS = 5_000; // 5 s → 10 s → 20 s
// Free-tier Gemini quota: 15 generateContent requests / 60 s / key.
// We keep a sliding window of timestamps and sleep the request until a slot
// opens, so bursts get paced rather than 429-ing. Retry still protects against
// external usage of the same key outside this process.
const RATE_LIMIT_RPM = 15;
const RATE_LIMIT_WINDOW_MS = 60_000;
const _rateTimestamps = [];
// Key: `${model}:${last8OfApiKey}` — re-validated every 10 min.
const _modelCache = new Map();
const MODEL_CACHE_TTL = 10 * 60_000;
// Key: FNV hash of url+model+mode — evicted after 5 min or when > 30 entries.
const _resultCache = new Map();
const RESULT_CACHE_TTL = 5 * 60_000;
const RESULT_CACHE_MAX = 30;
// —— Hashing ——
// FNV-1a 32-bit — fast, no imports needed, good enough for cache keys.
function fnv32(s) {
    let h = 0x811c9dc5;
    for(let i = 0; i < s.length; i++){
        h ^= s.charCodeAt(i);
        h = Math.imul(h, 0x01000193) >>> 0;
    }
    return h.toString(36);
}
// —— HTML preparation ——
function stripHtml(html) {
    const $ = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
    $("script, style, noscript, template, link[rel='stylesheet']").remove();
    $("svg").each((_, el)=>{
        const $el = $(el);
        const label = $el.attr("aria-label") ?? "";
        $el.replaceWith(`<svg${label ? ` aria-label="${label}"` : ""}>[icon]</svg>`);
    });
    const keepAttrs = new Set([
        "id",
        "class",
        "href",
        "src",
        "alt",
        "aria-label",
        "role",
        "data-component",
        "type"
    ]);
    $("*").each((_, el)=>{
        if (el.type !== "tag") return;
        for (const attr of Object.keys(el.attribs ?? {})){
            if (!keepAttrs.has(attr)) delete el.attribs[attr];
        }
    });
    return ($("body").html() ?? "").replace(/\s{2,}/g, " ").trim();
}
// —— Model pre-flight ——
// Fetch the list of generateContent-capable Gemini models for this key and
// pick the best available fallback: prefer flash (cheap/fast), then pro, then
// anything else. Returns null when the key has no usable model or the list
// call itself fails.
async function findFirstAvailableModel(apiKey) {
    try {
        const res = await __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].get(`https://generativelanguage.googleapis.com/v1beta/models?key=${apiKey}`, {
            timeout: 10_000
        });
        const models = (res.data.models ?? []).filter((m)=>m.supportedGenerationMethods?.includes("generateContent") && m.name.startsWith("models/gemini")).map((m)=>m.name.replace(/^models\//, "")).sort((a, b)=>{
            const tier = (v)=>v.includes("flash") ? 0 : v.includes("pro") ? 1 : 2;
            return tier(a) - tier(b);
        });
        return models[0] ?? null;
    } catch (err) {
        console.warn(`[llm-detector] findFirstAvailableModel failed: ${err instanceof Error ? err.message : String(err)}`);
        return null;
    }
}
async function checkModelAvailability(model, apiKey) {
    const cacheKey = `${model}:${apiKey.slice(-8)}`;
    const cached = _modelCache.get(cacheKey);
    if (cached && Date.now() - cached.ts < MODEL_CACHE_TTL) {
        return {
            ok: cached.ok,
            error: cached.error
        };
    }
    // The Gemini model resource path always uses the full "models/{id}" form.
    // Strip a "models/" prefix if the stored model ID somehow already has it.
    const modelId = model.replace(/^models\//, "");
    console.log(`[llm-detector] Pre-flight: checking model "${modelId}"…`);
    try {
        await __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].get(`https://generativelanguage.googleapis.com/v1beta/models/${modelId}?key=${apiKey}`, {
            timeout: 10_000
        });
        _modelCache.set(cacheKey, {
            ok: true,
            ts: Date.now()
        });
        console.log(`[llm-detector] Pre-flight: model "${modelId}" is available ✓`);
        return {
            ok: true
        };
    } catch (err) {
        let error;
        let status;
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].isAxiosError(err)) {
            status = err.response?.status;
            if (status === 404) {
                error = `Model "${modelId}" not found for this API key.`;
            } else if (status === 403) {
                error = `API key lacks access to "${modelId}".`;
            } else if (status === 429) {
                // Don't block on a rate-limited pre-flight — let the generation attempt proceed.
                console.warn(`[llm-detector] Pre-flight rate-limited (429), skipping model check`);
                _modelCache.set(cacheKey, {
                    ok: true,
                    ts: Date.now()
                });
                return {
                    ok: true
                };
            } else {
                error = `Model check failed (HTTP ${status ?? "network"}): ${err.message}`;
            }
        } else {
            error = `Model check failed: ${err instanceof Error ? err.message : String(err)}`;
        }
        console.error(`[llm-detector] Pre-flight: ${error}`);
        _modelCache.set(cacheKey, {
            ok: false,
            error,
            ts: Date.now()
        });
        return {
            ok: false,
            error,
            status
        };
    }
}
async function runGateCheck(html, config, url, mode) {
    // 1. API key must be present.
    if (!config.apiKey.trim()) {
        return {
            pass: false,
            reason: "No Gemini API key configured."
        };
    }
    // 2. Page must have enough content to be worth sending.
    const stripped = stripHtml(html);
    if (stripped.length < MIN_HTML_CHARS) {
        console.log(`[llm-detector] Gate rejected: stripped HTML only ${stripped.length} chars (min ${MIN_HTML_CHARS})`);
        return {
            pass: false,
            reason: `Page has too little content (${stripped.length} chars after stripping scripts/styles). ` + `Falling back to heuristics.`
        };
    }
    // 3. Deduplication: skip if we already processed this url+model+mode recently.
    const cacheKey = fnv32(`${url}|${config.model}|${mode}`);
    const cached = _resultCache.get(cacheKey);
    if (cached && Date.now() - cached.ts < RESULT_CACHE_TTL) {
        const ageS = Math.round((Date.now() - cached.ts) / 1000);
        console.log(`[llm-detector] Cache hit for url=${url} model=${config.model} (${ageS}s ago) — returning cached result`);
        return {
            pass: true,
            cacheHit: true,
            cachedComponents: cached.components
        };
    }
    // 4. Validate the selected model is reachable with this API key. If the
    //    configured model is unavailable (404/403), try to find any usable
    //    Gemini model and substitute rather than failing.
    const modelCheck = await checkModelAvailability(config.model, config.apiKey);
    if (modelCheck.ok) {
        return {
            pass: true,
            resolvedModel: config.model
        };
    }
    if (modelCheck.status === 404 || modelCheck.status === 403) {
        const fallback = await findFirstAvailableModel(config.apiKey);
        if (fallback && fallback !== config.model) {
            console.warn(`[llm-detector] Configured model "${config.model}" unavailable — substituting "${fallback}"`);
            // Warm the pre-flight cache for the fallback so we don't re-check it immediately.
            _modelCache.set(`${fallback}:${config.apiKey.slice(-8)}`, {
                ok: true,
                ts: Date.now()
            });
            return {
                pass: true,
                resolvedModel: fallback,
                notice: `Configured model "${config.model}" is not available for your key. Using "${fallback}" instead — saved to settings.`
            };
        }
    }
    return {
        pass: false,
        reason: modelCheck.error
    };
}
function cacheResult(url, model, mode, components) {
    const key = fnv32(`${url}|${model}|${mode}`);
    _resultCache.set(key, {
        components,
        ts: Date.now()
    });
    // LRU eviction: keep newest MAX entries.
    if (_resultCache.size > RESULT_CACHE_MAX) {
        const [oldest] = [
            ..._resultCache.entries()
        ].sort(([, a], [, b])=>a.ts - b.ts);
        if (oldest) _resultCache.delete(oldest[0]);
    }
}
// —— Prompt builders ——
const SCHEMA_EXAMPLE = `{
  "components": [
    {
      "id": "c_1_hero",
      "kind": "hero",
      "name": "Hero Section",
      "suggestedUid": "hero",
      "confidence": 0.92,
      "preview": "actual text preview under 160 chars",
      "selector": "section.hero",
      "fields": [
        { "uid": "headline", "displayName": "Headline", "type": "single_line", "value": "actual headline text", "required": true },
        { "uid": "primary_cta", "displayName": "Primary CTA", "type": "url", "value": { "title": "Get started", "href": "https://example.com/start" } }
      ]
    }
  ]
}`;
function buildDetectPrompt(strippedHtml, baseUrl) {
    return `You are a web component detector analyzing ${baseUrl}.

Identify distinct content sections/components. Skip navigation, header, and footer elements.

Valid kinds: ${VALID_KINDS.join(", ")}
Valid field types: ${VALID_FIELD_TYPES.join(", ")}

Rules:
- "url" fields: value = { "title": string, "href": string }
- "group" fields: include "itemSchema" (schema) and "value" (items array)
- "file" fields: value = image URL string
- Extract ACTUAL text from the HTML — no placeholders
- confidence: 0.35–0.99, suggestedUid: snake_case ≤40 chars

Return ONLY valid JSON:
${SCHEMA_EXAMPLE}

HTML:
${strippedHtml.slice(0, 30_000)}`;
}
function buildHybridPrompt(strippedHtml, baseUrl, heuristicComponents) {
    return `You are improving web component detection results for ${baseUrl}.

A structural heuristic detected these components. For each:
1. Confirm or correct the component kind
2. Adjust confidence (0.35–0.99) based on actual content
3. Replace placeholder field values with ACTUAL text from the HTML
4. Add significant content sections the heuristic missed

Keep existing "id" values. New components: use ids like "c_llm_1_hero".

Heuristic results:
${JSON.stringify(heuristicComponents, null, 2).slice(0, 6_000)}

Return ONLY valid JSON in the same schema. Include ALL components.

HTML:
${strippedHtml.slice(0, 24_000)}`;
}
// —— Core API call with retry ——
function sleep(ms) {
    return new Promise((r)=>setTimeout(r, ms));
}
// Block until the sliding window has room for another generateContent call,
// then reserve a slot. Safe for concurrent awaiters: each wake-up re-evaluates
// the window state atomically within a single event-loop turn.
async function waitForRateSlot() {
    // `while (true)` is intentional — we re-check after every sleep in case
    // multiple requests were queued and woke at the same time.
    while(true){
        const now = Date.now();
        while(_rateTimestamps.length > 0 && now - _rateTimestamps[0] > RATE_LIMIT_WINDOW_MS){
            _rateTimestamps.shift();
        }
        if (_rateTimestamps.length < RATE_LIMIT_RPM) {
            _rateTimestamps.push(now);
            return;
        }
        // Wait until the oldest timestamp ages out. +100 ms cushion to avoid
        // racing Google's window boundary.
        const waitMs = RATE_LIMIT_WINDOW_MS - (now - _rateTimestamps[0]) + 100;
        console.log(`[llm-detector] Rate limit full (${_rateTimestamps.length}/${RATE_LIMIT_RPM} in 60s) — waiting ${waitMs}ms for slot…`);
        await sleep(waitMs);
    }
}
async function callGemini(config, prompt) {
    // Strip "models/" prefix — the Gemini list API returns "models/gemini-1.5-flash"
    // but the generateContent endpoint wants just "gemini-1.5-flash".
    const model = (config.model || "gemini-1.5-flash").replace(/^models\//, "");
    const endpoint = `https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${config.apiKey}`;
    const promptTokenEstimate = Math.round(prompt.length / 4);
    console.log(`[llm-detector] Calling Gemini  model=${model}  prompt≈${promptTokenEstimate} tokens`);
    let lastError = null;
    for(let attempt = 0; attempt <= MAX_RETRIES; attempt++){
        if (attempt > 0) {
            const retryAfterSec = lastError && __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].isAxiosError(lastError) ? parseInt(lastError.response?.headers["retry-after"] ?? "0", 10) : 0;
            const waitMs = retryAfterSec > 0 ? retryAfterSec * 1_000 : BASE_BACKOFF_MS * Math.pow(2, attempt - 1);
            console.log(`[llm-detector] Retry ${attempt}/${MAX_RETRIES}: waiting ${waitMs}ms…`);
            await sleep(waitMs);
        }
        // Reserve an RPM slot before each attempt (including retries) — prevents
        // 429s from concurrent scans within the same process.
        await waitForRateSlot();
        try {
            const t0 = Date.now();
            const res = await __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].post(endpoint, {
                contents: [
                    {
                        parts: [
                            {
                                text: prompt
                            }
                        ]
                    }
                ],
                generationConfig: {
                    responseMimeType: "application/json",
                    temperature: 0.1,
                    maxOutputTokens: 8192
                }
            }, {
                timeout: 60_000
            });
            const text = res.data?.candidates?.[0]?.content?.parts?.[0]?.text ?? "";
            if (!text) {
                const reason = res.data?.candidates?.[0]?.finishReason ?? "unknown";
                throw new Error(`Empty response from Gemini (finishReason: ${reason}). ` + `This can happen when the prompt is flagged by safety filters.`);
            }
            const elapsed = Date.now() - t0;
            console.log(`[llm-detector] Gemini OK  ${elapsed}ms  response≈${Math.round(text.length / 4)} tokens`);
            const parsed = JSON.parse(text);
            if (!Array.isArray(parsed.components)) {
                throw new Error("Gemini returned JSON but it is missing the top-level `components` array.");
            }
            const components = parsed.components.map((c, i)=>normalizeComponent(c, i)).filter((c)=>c !== null);
            console.log(`[llm-detector] Normalised ${parsed.components.length} raw → ${components.length} valid components`);
            return components;
        } catch (err) {
            lastError = err instanceof Error ? err : new Error(String(err));
            if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].isAxiosError(err)) {
                const status = err.response?.status;
                const body = JSON.stringify(err.response?.data ?? {}).slice(0, 300);
                if (status === 404) {
                    // Already caught by the gate check, but handle here defensively.
                    console.error(`[llm-detector] 404 — model "${model}" not found. ${body}`);
                    throw new Error(`Model "${model}" not found (404). ` + `Open AI Settings and switch to Gemini 1.5 Flash.`);
                }
                if (status === 403) {
                    console.error(`[llm-detector] 403 — API key invalid or lacks permission. ${body}`);
                    throw new Error(`API key rejected (403). ` + `Verify the key in AI Settings or re-verify with the "Test key" button.`);
                }
                if (status === 400) {
                    console.error(`[llm-detector] 400 — bad request. ${body}`);
                    throw new Error(`Gemini rejected the request (400). ${body}`);
                }
                if (status === 429) {
                    console.warn(`[llm-detector] 429 rate limit (attempt ${attempt + 1}/${MAX_RETRIES + 1})`);
                    if (attempt < MAX_RETRIES) continue;
                    throw new Error(`Gemini rate limit exceeded after ${MAX_RETRIES + 1} attempts. ` + `Free tier allows 15 req/min — try again in a moment, or upgrade to a paid plan.`);
                }
                // 5xx or network: retry
                console.warn(`[llm-detector] HTTP ${status ?? "network"} error (attempt ${attempt + 1}). ${body}`);
                if (attempt < MAX_RETRIES) continue;
            } else {
                // Parse error, schema error, etc. — not retryable.
                console.error(`[llm-detector] Non-HTTP error (attempt ${attempt + 1}): ${lastError.message}`);
                throw lastError;
            }
        }
    }
    throw lastError ?? new Error("LLM call failed after all retries.");
}
async function detectComponentsWithLlm(html, baseUrl, config) {
    console.log(`[llm-detector] Mode=llm  url=${baseUrl}`);
    // Gate check — validates key, content size, model, and deduplication.
    const gate = await runGateCheck(html, config, baseUrl, "llm");
    if (!gate.pass) {
        console.log(`[llm-detector] Gate rejected: ${gate.reason}`);
        return {
            components: [],
            fallbackUsed: true,
            error: gate.reason
        };
    }
    const effectiveConfig = gate.resolvedModel ? {
        ...config,
        model: gate.resolvedModel
    } : config;
    if (gate.cacheHit) {
        return {
            components: gate.cachedComponents,
            fallbackUsed: false,
            modelUsed: effectiveConfig.model,
            notice: gate.notice
        };
    }
    const stripped = stripHtml(html);
    try {
        const components = await callGemini(effectiveConfig, buildDetectPrompt(stripped, baseUrl));
        cacheResult(baseUrl, effectiveConfig.model, "llm", components);
        console.log(`[llm-detector] LLM detection succeeded: ${components.length} components`);
        return {
            components,
            fallbackUsed: false,
            modelUsed: effectiveConfig.model,
            notice: gate.notice
        };
    } catch (err) {
        const error = err instanceof Error ? err.message : "LLM detection failed.";
        console.error(`[llm-detector] LLM detection failed after all retries: ${error}`);
        return {
            components: [],
            fallbackUsed: false,
            error
        };
    }
}
async function detectComponentsHybrid(html, baseUrl, heuristicComponents, config) {
    console.log(`[llm-detector] Mode=hybrid  url=${baseUrl}  heuristic=${heuristicComponents.length} components`);
    const gate = await runGateCheck(html, config, baseUrl, "hybrid");
    if (!gate.pass) {
        console.log(`[llm-detector] Gate rejected: ${gate.reason}`);
        return {
            components: heuristicComponents,
            fallbackUsed: true,
            error: gate.reason
        };
    }
    const effectiveConfig = gate.resolvedModel ? {
        ...config,
        model: gate.resolvedModel
    } : config;
    if (gate.cacheHit) {
        return {
            components: gate.cachedComponents,
            fallbackUsed: false,
            modelUsed: effectiveConfig.model,
            notice: gate.notice
        };
    }
    const stripped = stripHtml(html);
    try {
        const components = await callGemini(effectiveConfig, buildHybridPrompt(stripped, baseUrl, heuristicComponents));
        cacheResult(baseUrl, effectiveConfig.model, "hybrid", components);
        console.log(`[llm-detector] Hybrid refinement succeeded: ${components.length} components`);
        return {
            components,
            fallbackUsed: false,
            modelUsed: effectiveConfig.model,
            notice: gate.notice
        };
    } catch (err) {
        const error = err instanceof Error ? err.message : "LLM refinement failed.";
        console.error(`[llm-detector] Hybrid refinement failed after all retries: ${error}`);
        return {
            components: heuristicComponents,
            fallbackUsed: true,
            error
        };
    }
}
async function testGeminiApiKey(apiKey) {
    try {
        await __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].get(`https://generativelanguage.googleapis.com/v1beta/models?key=${apiKey}`, {
            timeout: 10_000
        });
        return {
            ok: true
        };
    } catch (err) {
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].isAxiosError(err)) {
            const status = err.response?.status;
            if (status === 400 || status === 403) return {
                ok: false,
                error: "Invalid API key."
            };
            if (status === 429) return {
                ok: false,
                error: "Rate limit reached — key is valid but quota exhausted."
            };
        }
        return {
            ok: false,
            error: err instanceof Error ? err.message : "Connection failed."
        };
    }
}
// —— Normalizers ——
function normalizeComponent(raw, index) {
    if (!raw || typeof raw !== "object") return null;
    const c = raw;
    const kind = VALID_KINDS.includes(c.kind) ? c.kind : "rich_text";
    const id = typeof c.id === "string" ? c.id : `c_${index + 1}_${kind}`;
    const name = typeof c.name === "string" ? c.name : kind;
    const suggestedUid = typeof c.suggestedUid === "string" ? c.suggestedUid.slice(0, 40) : kind;
    const confidence = typeof c.confidence === "number" ? Math.min(0.99, Math.max(0.35, c.confidence)) : 0.7;
    const preview = typeof c.preview === "string" ? c.preview.slice(0, 160) : "";
    const selector = typeof c.selector === "string" ? c.selector : "section";
    const fields = Array.isArray(c.fields) ? c.fields.map(normalizeField).filter((f)=>f !== null) : [];
    if (!fields.length) return null;
    return {
        id,
        kind,
        name,
        suggestedUid,
        confidence,
        preview,
        selector,
        fields
    };
}
function normalizeField(raw) {
    if (!raw || typeof raw !== "object") return null;
    const f = raw;
    const type = VALID_FIELD_TYPES.includes(f.type) ? f.type : "single_line";
    const uid = typeof f.uid === "string" ? f.uid : "field";
    const displayName = typeof f.displayName === "string" ? f.displayName : uid;
    const out = {
        uid,
        displayName,
        type,
        value: f.value ?? ""
    };
    if (f.required === true) out.required = true;
    if (Array.isArray(f.itemSchema)) {
        out.itemSchema = f.itemSchema.map(normalizeField).filter((x)=>x !== null);
    }
    if (typeof f.assetUrl === "string") out.assetUrl = f.assetUrl;
    return out;
}
}),
"[project]/src/app/api/scrape/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "POST",
    ()=>POST,
    "runtime",
    ()=>runtime
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/axios/lib/axios.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/load-parse.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/detector.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$llm$2d$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/llm-detector.ts [app-route] (ecmascript)");
;
;
;
;
;
const runtime = "nodejs";
async function POST(request) {
    let body;
    try {
        body = await request.json();
    } catch  {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            ok: false,
            url: "",
            components: [],
            error: "invalid_json"
        }, {
            status: 400
        });
    }
    const url = body.url?.trim();
    if (!url) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            ok: false,
            url: "",
            components: [],
            error: "missing_url"
        }, {
            status: 400
        });
    }
    try {
        const res = await __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].get(url, {
            timeout: 20_000,
            responseType: "text",
            headers: {
                "User-Agent": "Mozilla/5.0 (compatible; StackshiftBot/0.1; +https://stackshift.dev)",
                Accept: "text/html,application/xhtml+xml"
            },
            maxRedirects: 5
        });
        const html = res.data;
        const $ = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
        const title = $("title").first().text().trim() || undefined;
        const mode = body.detectionMode ?? "heuristic";
        const llm = body.llmConfig;
        const llmReady = !!(llm?.enabled && llm.apiKey.trim());
        console.log(`[scrape] url=${url} mode=${mode} llmReady=${llmReady} htmlSize=${html.length}`);
        // ——— LLM-only mode ———
        if (mode === "llm" && llmReady) {
            const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$llm$2d$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["detectComponentsWithLlm"])(html, url, llm);
            if (result.error && !result.components.length) {
                // All retries exhausted — fall back to heuristic.
                console.warn(`[scrape] LLM failed, falling back to heuristic. Error: ${result.error}`);
                const components = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["detectComponents"])(html, url);
                return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                    ok: true,
                    url,
                    title,
                    components,
                    detectionMethod: "heuristic_fallback",
                    llmWarning: result.error
                });
            }
            console.log(`[scrape] LLM succeeded: ${result.components.length} components returned  modelUsed=${result.modelUsed}`);
            const warning = result.notice ?? result.error;
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                ok: true,
                url,
                title,
                components: result.components,
                detectionMethod: "llm",
                ...result.modelUsed ? {
                    modelUsed: result.modelUsed
                } : {},
                ...warning ? {
                    llmWarning: warning
                } : {}
            });
        }
        // ——— Hybrid mode ———
        if (mode === "hybrid" && llmReady) {
            const heuristicComponents = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["detectComponents"])(html, url);
            console.log(`[scrape] Hybrid: heuristic found ${heuristicComponents.length} components, sending to LLM for refinement`);
            const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$llm$2d$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["detectComponentsHybrid"])(html, url, heuristicComponents, llm);
            console.log(`[scrape] Hybrid done: ${result.components.length} components, fallback=${result.fallbackUsed}, modelUsed=${result.modelUsed}`);
            const warning = result.notice ?? result.error;
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                ok: true,
                url,
                title,
                components: result.components,
                detectionMethod: result.fallbackUsed ? "heuristic_fallback" : "hybrid",
                ...result.modelUsed ? {
                    modelUsed: result.modelUsed
                } : {},
                ...warning ? {
                    llmWarning: warning
                } : {}
            });
        }
        // ——— Default: heuristic ———
        const components = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$detector$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["detectComponents"])(html, url);
        console.log(`[scrape] Heuristic: ${components.length} components`);
        const detectionMethod = (mode === "llm" || mode === "hybrid") && !llmReady ? "heuristic_fallback" : "heuristic";
        const llmWarning = (mode === "llm" || mode === "hybrid") && !llmReady ? "AI detection requires a configured Gemini API key. Using heuristics instead." : undefined;
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            ok: true,
            url,
            title,
            components,
            detectionMethod,
            ...llmWarning ? {
                llmWarning
            } : {}
        });
    } catch (err) {
        const message = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$axios$2f$lib$2f$axios$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].isAxiosError(err) && err.message ? err.message : err instanceof Error ? err.message : "scrape_failed";
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            ok: false,
            url,
            components: [],
            error: message
        }, {
            status: 502
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__0kz9map._.js.map